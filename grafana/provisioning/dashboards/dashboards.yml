apiVersion: 1

# Dashboard provisioning - это автоматическая загрузка dashboard JSON при старте Grafana.
# Без provisioning dashboard пришлось бы импортировать вручную через UI.
# Dashboard'ы в ADS-2 demo создаются через Grafana API/MCP.
# Файловый provisioning отключен, чтобы он не конфликтовал с dashboard'ами,
# которые Kimi создает динамически во время анализа.
providers: []
